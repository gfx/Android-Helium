machine:
    services:
        - docker

dependencies:
    override:
        - docker build -t Android-Helium .

test:
    override:
        - docker run -it Android-Helium ./gradlew --stacktrace test build
