machine:
    services:
        - docker

dependencies:
    override:
        - docker build -t android_helium .

test:
    override:
        - docker run -it android_helium ./gradlew --stacktrace test build
